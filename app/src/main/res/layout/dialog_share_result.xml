<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp"
    >

    <TextView
        android:id="@+id/tv_share_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/share.title"
        android:textSize="16sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        />

    <ImageView
        android:id="@+id/iv_share_facebook"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginTop="16dp"
        app:layout_constraintEnd_toStartOf="@id/iv_share_twitter"
        app:layout_constraintHorizontal_chainStyle="spread"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tv_share_title"
        app:srcCompat="@drawable/ic_facebook"
        tools:ignore="ContentDescription"
        />

    <ImageView
        android:id="@+id/iv_share_twitter"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginTop="16dp"
        app:layout_constraintEnd_toStartOf="@id/iv_share_email"
        app:layout_constraintHorizontal_chainStyle="spread"
        app:layout_constraintStart_toEndOf="@id/iv_share_facebook"
        app:layout_constraintTop_toBottomOf="@id/tv_share_title"
        app:srcCompat="@drawable/ic_twitter"
        tools:ignore="ContentDescription"
        />

    <ImageView
        android:id="@+id/iv_share_email"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginTop="16dp"
        app:layout_constraintEnd_toStartOf="@id/iv_share_etc"
        app:layout_constraintHorizontal_chainStyle="spread"
        app:layout_constraintStart_toEndOf="@id/iv_share_twitter"
        app:layout_constraintTop_toBottomOf="@id/tv_share_title"
        app:srcCompat="@drawable/ic_twitter"
        tools:ignore="ContentDescription"
        />

    <ImageView
        android:id="@+id/iv_share_etc"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginTop="16dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_chainStyle="spread"
        app:layout_constraintStart_toEndOf="@id/iv_share_email"
        app:layout_constraintTop_toBottomOf="@id/tv_share_title"
        app:srcCompat="@drawable/ic_twitter"
        tools:ignore="ContentDescription"
        />

</android.support.constraint.ConstraintLayout>